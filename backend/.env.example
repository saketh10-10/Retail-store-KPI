# ========================================
# IMPORTANT: Copy this file to .env and configure with your own values
# DO NOT commit .env file to Git (it's in .gitignore)
# ========================================

# Server Configuration
PORT=5000

# Weather API (for existing weather functionality)
# Get your free API key from: https://openweathermap.org/api
WEATHER_API_KEY=your_openweather_api_key_here

# JWT Secret for authentication (use a strong random string)
# IMPORTANT: Change this in production!
JWT_SECRET=retail-kpi-secret-key-2024-change-this-in-production

# Database (SQLite file will be created automatically)
# No additional database config needed for SQLite

# ========================================
# EMAIL CONFIGURATION (REQUIRED for email alerts)
# ========================================
# This is the Gmail account that SENDS emails (SMTP server)
# NOT the manager's email - managers enter their email during login
# 
# For Gmail:
# 1. Go to: https://myaccount.google.com/apppasswords
# 2. Enable 2-Step Verification if not already enabled
# 3. Generate an App Password for "Mail"
# 4. Use that 16-character password below (remove spaces)
#
# For other email providers, check their SMTP settings
# ========================================

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password-here

# ========================================
# NOTE: 
# - If email is not configured, alerts will be logged to console only
# - Low stock alerts are sent when product quantity falls below threshold (default: 10)
# - Managers enter their own email during login to receive alerts
# ========================================
